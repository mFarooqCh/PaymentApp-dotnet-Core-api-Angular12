## About PaymentAPI Appliction

Build using ASP.NET Core (5.0) Web API, Angular 12 and Bootstrap 5.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory.
